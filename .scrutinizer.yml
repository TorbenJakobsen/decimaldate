build:
  nodes:
    coverage:
      tests:
        override:
          - command: '$(COVERAGE) run -m pytest tests'
            coverage:
              file: '.coverage'
              config_file: '.coveragerc'
              format: 'py-cc'