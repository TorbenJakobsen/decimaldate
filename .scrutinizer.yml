build:
  nodes:
    coverage:
      tests:
        override:
          - command: 'make test'
            coverage:
              file: '.coverage'
              config_file: '.coveragerc'
              format: 'py-cc'